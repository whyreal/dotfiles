#!/usr/bin/env python3

import os.path

if not os.path.isdir(".vim"):
    os.mkdir(".vim")

if not os.path.isfile(".vim/workspace"):
    ws = open(".vim/workspace", "w")
    ws.close()
    # os.mknod(".vim/workspace")
