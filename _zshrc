# oh-my-zsh {{{1
# Path to your oh-my-zsh configuration.
export ZSH=$HOME/.oh-my-zsh
# Set name of the theme to load.
# Look in ~/.oh-my-zsh/themes/
# Optionally, if you set this to "random", it'll load a random theme each
# time that oh-my-zsh is loaded.
ZSH_THEME="candy"

# Set to this to use case-sensitive completion
# CASE_SENSITIVE="true"

# Comment this out to disable bi-weekly auto-update checks
DISABLE_AUTO_UPDATE="true"

# Uncomment to change how often before auto-updates occur? (in days)
# export UPDATE_ZSH_DAYS=13

# Uncomment following line if you want to disable colors in ls
# DISABLE_LS_COLORS="true"

# Uncomment following line if you want to disable autosetting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment following line if you want to disable command autocorrection
DISABLE_CORRECTION="true"

# Uncomment following line if you want red dots to be displayed while waiting for completion
# COMPLETION_WAITING_DOTS="true"

# Uncomment following line if you want to disable marking untracked files under
# VCS as dirty. This makes repository status check for large repositories much,
# much faster.
# DISABLE_UNTRACKED_FILES_DIRTY="true"

# Which plugins would you like to load? (plugins can be found in ~/.oh-my-zsh/plugins/*)
# Custom plugins may be added to ~/.oh-my-zsh/custom/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
plugins=(osx)

source $ZSH/oh-my-zsh.sh

#bindkey -v
fpath=(/usr/local/share/zsh-completions $fpath)

# }}}
# ALIAS {{{1
alias o='open'
alias grep='grep --color'
alias ls='ls -G'
alias pg='ps aux | grep'
alias s='sshrc'
alias ....='cd ../../..'
alias ...='cd ../..'
alias ..='cd ..'
alias e='open -t'
alias plantuml='java -jar /Applications/plantuml.jar'
alias note='vim -c "Note"'
alias ssh.add_key.zzzw='ssh-add ~/.ssh/zzzw_inet'
alias ssh.tunnel.wjs.socks='ssh -NfD 10000 wjs.129.1.0.112'
alias ssh.tunnel.wjs.win='ssh -NfL 3389:129.1.0.2:3389 zkzy.192.168.80.99'
alias npm='npm --no-optional'
# }}}
# ENV VARIABLES {{{1
# fix git problem for https
export GIT_SSL_NO_VERIFY=true
# history
export HISTSIZE=5000
export HISTFILESIZE=50000
# WORDCHARS
#export WORDCHARS="@*?-.[]~/&;!#$%^(){}<>"
# editor
export EDITOR='vim'
export LC_ALL="en_US.UTF-8"
# use vim as man pager
export MANPAGER="col -b | vim -c 'set ft=man nomodifiable' -"

#export LC_ALL="zh_CN.GB2312"
if [[ $LC_CTYPE = "zh_CN.eucCN" ]]; then
    export NLS_LANG=AMERICAN_AMERICA.ZHS16GBK
fi

# color
export CLICOLOR=1
export LSCOLORS=GxFxCxDxBxegedabagaced
#export LSCOLORS='gxfxcxdxbxegedabagacad'
export PATH="/usr/local/sbin:/usr/local/bin:$PATH"

# Oracle
export ORACLE_HOME=/Applications/instantclient_12_2/
export TNS_ADMIN=${ORACLE_HOME}/network/admin
export PATH=${ORACLE_HOME}:/usr/local/ActivePerl-5.20/bin:${PATH}
export DYLD_LIBRARY_PATH=${ORACLE_HOME}:${DYLD_LIBRARY_PATH}

# Maven Wrap
#export PATH=~/.mvnw/bin:$PATH

export PATH=/Users/Real/Documents/Note/scripts/:$PATH

# MacVIM
export PATH=/Applications/MacVim.app/Contents/bin/:$PATH

# mysql
export PATH=/usr/local/opt/mysql-client/bin:$PATH

# golang
export PATH=/Users/Real/go/bin:$PATH
export GOPATH=/Users/Real/go

###################
# python virtualenv
###################

export PATH=/Users/Real/Library/Python/3.7/bin/:$PATH

#export PATH=/Users/Real/.local/bin:$PATH
#export WORKON_HOME=$HOME/.virtualenvs
#export VIRTUALENVWRAPPER_PYTHON=/Users/Real/.pyenv/shims/python
#source /usr/local/bin/virtualenvwrapper.sh

################
# pyenv & pipenv
################

#export LDFLAGS="-L/usr/local/opt/zlib/lib -L/usr/local/opt/sqlite/lib -L/usr/local/opt/readline/lib"
#export CPPFLAGS="-I/usr/local/opt/zlib/include -I/usr/local/opt/readline/include -I/usr/local/opt/sqlite/include"
#export PKG_CONFIG_PATH="/usr/local/opt/zlib/lib/pkgconfig /usr/local/opt/sqlite/lib/pkgconfig"

#eval "$(pyenv init -)"
#if which pyenv-virtualenv-init > /dev/null; then eval "$(pyenv virtualenv-init -)"; fi
#export PYENV_VIRTUALENV_DISABLE_PROMPT=1


#}}}
# vim: fdm=marker sw=4 ts=4 et
